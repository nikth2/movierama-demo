steps:
- name: 'gcr.io/cloud-builders.docker'
  args: [ 'build', '-t', 'europe-west2-docker.pkg.dev/$PROJECT_ID/quickstart-docker-repo/quickstart-image:tag1', '.']
images:
- 'europe-west2.docker.pkg.dev/$PROJECT_ID/quickstart-docker-repo/quickstart-image:tag1'
